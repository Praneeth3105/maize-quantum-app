streamlit
tensorflow==2.20.0 
numpy==2.2.6
scikit-learn
joblib
opencv-python-headless
pennylane
firebase-admin
protobuf


// Note: If the specified requirements are not compatible with the particular version being used, we should upgrade/change to a newer compatible version and proceed with that version instead.
