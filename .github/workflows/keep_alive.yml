name: Debug Streamlit

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Show headers
        run: |
          curl -I -v "https://maize-quantum-app-6jmirwun4wa2aokjqnshgh.streamlit.app/"
