name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/5 * * * *"    # Every 5 minutes
  workflow_dispatch:

jobs:
  keep_alive:
    runs-on: ubuntu-latest

    steps:
      - name: Wake Streamlit
        run: |
          curl -L "https://maize-quantum-app-6jmirwun4wa2aokjqnshgh.streamlit.app/"
